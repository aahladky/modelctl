---
description: Run one or more specific test modules, e.g. /test-one test_modelctl_tiers
---

Run only these test module(s) from the repo root with `-v`:

$ARGUMENTS

Use `.venv/bin/python -m unittest <modules> -v`. Report failures with their tracebacks.
